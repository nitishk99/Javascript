"use strict"; // treat all JS code as newer version (now even this is not nessesary engines can auromatically detect)

// alert( 3 + 3) // we are using nodejs it will give error, not browser 

console.log(3 
    +
     3) // code readability should be high

console.log("Nitish")


let name = "Nitish"
let age = 18
let isLoggedIn = false
let state;

// number => 2 to power 53
// bigint
// string => ""
// boolean => true/false
// null => standalone value
// undefined => 
// symbol => unique


// object

console.log(typeof undefined); // undefined
console.log(typeof null); // object
